package orien.tools {
	/**
	 * The namespace used to enclose properties and methods that should only be available within the OrienTools library.
	 * This keeps the public API clean and prevents the calling of methods that should never be called manually.
	 */
    public namespace orien;
}